# dummy_robot_skills.yaml
# Capabilities available to the dummy_robot in this deployment.
# This file is passed to test_start_mission via the 'skills_file' launch argument
# and its contents are forwarded to every plan/replan request so the LLM
# plans exclusively within this capability envelope.
#
# Usage:
#   ros2 launch dummy_robot dummy_robot_llm.launch.py \
#     skills_file:=$(ros2 pkg prefix dummy_robot)/share/dummy_robot/config/dummy_robot_skills.yaml

skills:
  - "Speak using text-to-speech"
  - "Ask an open-ended question and capture the answer"
  - "Ask a yes/no confirmation question and capture the result"
  - "Check whether requested items are available against a provided availability list"
  - "Detect and identify a target entity (person, object)"
  - "Set the perception system to search for a specific target class"
  - "Navigate to a named location in the environment"
  - "Navigate to a dynamic destination provided via blackboard (e.g. {detected_person}, {extracted_location})"
  - "Navigate to a location specified by map coordinates"
  - "Obtain the navigation frame given a location name, destination description, or routing category (e.g. visit_reason -> destination frame)"
  - "Wait for a specified duration"
  - "Execute a generic action (e.g. open door, pick object, send notification)"
  - "Enumerate and speak a list of items using text-to-speech"
  - "Extract semantic information fields from free text using LLM (names, items, intents), excluding navigation-frame resolution"
  - "Spin in place"
  - "Follow a moving person or entity to maintain relative distance (tracking behavior, not destination-leading navigation)"
  - "Estimate relative geometry to a detected target frame (e.g. distance and bearing) for tracking and control decisions"
  - "Evaluate relative tracking conditions against a target frame (e.g. orientation alignment and proximity thresholds)"
  - "Execute reactive local motion primitives for tracking (e.g. rotate-to-heading and controlled forward motion)"
